# scikit-learn va fijo: el modelo se serializó con esta versión y una distinta
# puede fallar al deserializar o cambiar el resultado en silencio.
scikit-learn==1.9.0

# El resto con mínimos, para que el resolvedor de la nube tenga margen.
streamlit>=1.40
pandas>=2.2
numpy>=1.26
plotly>=5.20
joblib>=1.3
openpyxl>=3.1

# Solo para regenerar el parquet con src/extract_data.py; la app no los usa.
duckdb>=1.0
pyarrow>=15.0
